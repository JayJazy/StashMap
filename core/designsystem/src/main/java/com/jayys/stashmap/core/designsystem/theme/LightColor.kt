package com.jayys.stashmap.core.designsystem.theme

import androidx.compose.ui.graphics.Color

val LightColors = StashMapColors(
    // Grayscale
    grayLight1 = Color(0xFFF5F5F5),     // 가장 밝은 회색
    grayLight2 = Color(0xFFE0E0E0),     // 밝은 회색
    grayDark1 = Color(0xFF757575),      // 어두운 회색
    grayDark2 = Color(0xFF424242),      // 가장 어두운 회색

    // Purple
    purpleLight1 = Color(0xFFE1BEE7),   // 가장 밝은 보라
    purpleLight2 = Color(0xFFBA68C8),   // 밝은 보라
    purpleDark1 = Color(0xFF7B1FA2),    // 어두운 보라
    purpleDark2 = Color(0xFF4A148C),    // 가장 어두운 보라

    // Pink
    pinkLight1 = Color(0xFFF8BBD0),     // 가장 밝은 핑크
    pinkLight2 = Color(0xFFF06292),     // 밝은 핑크
    pinkDark1 = Color(0xFFC2185B),      // 어두운 핑크
    pinkDark2 = Color(0xFF880E4F),      // 가장 어두운 핑크

    // Red
    redLight1 = Color(0xFFFFCDD2),      // 가장 밝은 빨강
    redLight2 = Color(0xFFEF5350),      // 밝은 빨강
    redDark1 = Color(0xFFD32F2F),       // 어두운 빨강
    redDark2 = Color(0xFFB71C1C),       // 가장 어두운 빨강

    // Orange
    orangeLight1 = Color(0xFFFFE0B2),   // 가장 밝은 주황
    orangeLight2 = Color(0xFFFFB74D),   // 밝은 주황
    orangeDark1 = Color(0xFFF57C00),    // 어두운 주황
    orangeDark2 = Color(0xFFE65100),    // 가장 어두운 주황

    // Yellow
    yellowLight1 = Color(0xFFFFF9C4),   // 가장 밝은 노랑
    yellowLight2 = Color(0xFFFFEE58),   // 밝은 노랑
    yellowDark1 = Color(0xFFFDD835),    // 어두운 노랑
    yellowDark2 = Color(0xFFF57F17),    // 가장 어두운 노랑

    // Green
    greenLight1 = Color(0xFFC8E6C9),    // 가장 밝은 초록
    greenLight2 = Color(0xFF66BB6A),    // 밝은 초록
    greenDark1 = Color(0xFF388E3C),     // 어두운 초록
    greenDark2 = Color(0xFF1B5E20),     // 가장 어두운 초록

    // Blue
    blueLight1 = Color(0xFFBBDEFB),     // 가장 밝은 파랑
    blueLight2 = Color(0xFF42A5F5),     // 밝은 파랑
    blueDark1 = Color(0xFF1976D2),      // 어두운 파랑
    blueDark2 = Color(0xFF0D47A1),      // 가장 어두운 파랑

    // Teal
    tealLight1 = Color(0xFFB2DFDB),     // 가장 밝은 청록
    tealLight2 = Color(0xFF26A69A),     // 밝은 청록
    tealDark1 = Color(0xFF00796B),      // 어두운 청록
    tealDark2 = Color(0xFF004D40),      // 가장 어두운 청록

    // Common
    bgColor = Color(0xFFFFFFFF),
    baseColor = Color(0xFF000000),
)